# 聊天记录: AI (2026-04-26 16:25)

### 当前模型: fackgemini|gemini-3-flash-preview

### 系统提示词

> 你是一个AI助手

---

### 👤 用户 - *2026-04-26 15:50*

> 用问答的方式引导我完善这个主页的设计，需要补全，如果拿这个设计给ai agent开发就全是不符合预期的效果甚至报错
> 
> 这是我思考的模板
> 
> 你不需要更改项目代码，你在这里只需要引导我的思考，记录成文档
> 
> 完善feature>home 的逻辑
> 
> - 第一：当前无行为空页面显示了4个带加号的网格
> 
> - 期望：显示一个空加号网格（占据1/4宽度）
> 
> - 相当于4个行为正常形式，但是右边3个隐形，这样的占位空间
> 
> - 添加一个活动后
> 
> - 显示一个行为网格右侧排列一个空加号网格
> 
> - 第二：当前点击加号网格后，活动列为空，无标签列，无法添加活动，无法添加标签
> 
> - 期望：增加+号，点击可调用数据增加逻辑
> 
> - 显示标签区
> 
> - 点击标签区标题文字
> 
> - 弹出添加标签窗口
> 
> - 当前只有活动区
> 
> - 点击活动区标题文字
> 
> - 弹出添加活动窗口
> 
> - 第三：添加目标的逻辑
> 
> - 目标为顺位线性添加，不可控制开始与结束时间，可以有一个预计时间占位
> 
> - 第四：已完成行为的添加逻辑
> 
> - 结束时间不可超过当前时间
> 
> - 完善相应的数据库增删改查函数

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 15:50*

> 为了让 AI Agent 能够精准地开发出你想要的功能，我们需要将这些模糊的意图转化为**结构化的逻辑规范**。
> 
> 我会分模块向你提问，请你根据直觉或已有构思回答。我会根据你的回答实时整理出一份“AI 开发者任务说明书”。
> 
> ---
> 
> ### 第一阶段：布局与状态流转（Grid & Empty State）
> 
> 当前你的设计是“1+3”模式（一个可见，三个占位），我们需要定义清晰的边界条件。
> 
> **问题 1：关于“占位空间”的渲染逻辑**
> *   当没有任何活动时，页面是只显示 1 个加号网格，还是 1 个加号网格 + 3个完全透明但占据空间的 Div？
> *   **追问：** 如果用户添加了 4 个活动，第 5 个活动应该出现在哪里？是换行，还是这个“加号网格”永远排在最后一个活动后面，形成一个动态序列？
> 
> **问题 2：网格的大小和响应式**
> *   你提到的“1/4 宽度”是在什么尺寸下（手机端还是网页端）？如果屏幕变窄，它是保持 4 列还是会自动变成 2 列？
> 
> ---
> 
> ### 第二阶段：交互入口与弹窗逻辑（Interaction & Modals）
> 
> 我们需要明确“标签”和“活动”的归属关系。
> 
> **问题 3：标签（Label）与活动（Activity）的层级关系**
> *   点击“标签区标题”弹出的添加窗口，是创建一个全局可用的标签，还是仅限当前主页显示的标签？
> *   **追问：** 一个“活动”必须关联一个“标签”吗？还是说可以先创建活动，后续再打标签？
> 
> **问题 4：加号网格的行为意图**
> *   点击那个“空加号网格”时，默认行为应该是弹出“添加活动”窗口，还是“添加标签”窗口？还是弹出一个菜单让你二选一？
> 
> ---
> 
> ### 第三阶段：目标逻辑（Goal Logic - Linear Flow）
> 
> 这里涉及到核心的业务算法。
> 
> **问题 5：线性顺位的具体表现**
> *   “不可控制开始与结束时间”意味着系统会自动根据前一个目标的完成情况来排期吗？
> *   **追问：** “预计时间占位”是以小时为单位，还是天？如果前一个目标延期了，后面的目标是自动顺延，还是保持不动仅提示过期？
> 
> ---
> 
> ### 第四阶段：数据约束与校验（Constraints & Database）
> 
> 这是防止 AI 写出 Bug 的关键。
> 
> **问题 6：时间轴的死线**
> *   对于“已完成行为”，如果用户手动输入了一个未来的结束时间，系统是直接报错拦截，还是自动将时间修正为当前的 `Date.now()`？
> 
> **问题 7：数据库字段补全**
> *   除了 ID 和名称，你认为一个“活动”对象（Activity Object）必须包含哪些字段？（例如：颜色、图标、关联标签 ID、创建时间、累计耗时等）
> 
> ---
> 
> **请你先尝试回答这些问题（可以分条回答），我会根据你的反馈为你生成第一版《feature>home 逻辑详述文档》。**

---

### 👤 用户 - *2026-04-26 15:50*

> 问题一：页面是只显示 1 个加号网格

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 15:50*

> 发生错误: Request was aborted.

---

### 👤 用户 - *2026-04-26 16:01*

> 问题 1：关于“占位空间”的渲染逻辑
> - 只显示 1 个加号网格
> - 追问
>   - 换行，并且“加号网格”永远排在最后一个活动后面，形成一个动态序列
> 问题 2：网格的大小和响应式
> - 网格默认布局是四列，所以是总空间的4分之一，不在默认布局考虑屏幕变窄，未来有另外的模式可以适配
> 问题 3：标签（Label）与活动（Activity）的层级关系
> - 全局
> - 追问
>     - 不必须，可以创建，也可以补标签
> 问题 4：加号网格的行为意图
> - 是全新的叙事窗口:行为。行为相当于任务，是活动与标签的共同解释，所以弹出窗口中可以选择单个活动，多个标签。可以输入备注
> 问题 5：线性顺位的具体表现
> - 目标不可控制时间意味着，你只有完成了前一个行为才能开始这个目标同时开启计时，让他成为当前行为，但是程序要记录预计时间以及花费时间
> - 追问
>     - 目标不会延期，目标不会过期，目标只是下一个预订行为，预计时间是目标所花费的预计时间，一般任务较短，所以以分钟为单位
> 问题 6：时间轴的死线
> - 修正
> 问题 7：数据库字段补全
> - 颜色，图标，创建时间，是否归档，累计耗时，归档时间，绑定标签ID列表，活动备注，默认预计花费时间

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 16:01*

> 基于你的回答，我已经为你整理出了一份详尽的 **《feature>home 业务逻辑开发规范》**。这份文档可以直接作为提示词（Prompt）交给 AI Agent，或者作为你开发时的逻辑基准。
> 
> ---
> 
> # 业务逻辑规范文档：Home 模块 (feature>home)
> 
> ## 1. 布局与渲染逻辑 (Grid System)
> *   **网格容器**：采用固定 4 列（4-column grid）布局，暂不考虑移动端响应式。
> *   **动态序列**：
>     *   **初始状态**：页面仅渲染一个“加号网格”，占据第一行第一列（1/4 宽度），后三格为空白（但不塌陷，保持占位）。
>     *   **增长逻辑**：新添加的“行为（Behavior）”按顺序排列，“加号网格”始终作为序列的最后一个元素。
>     *   **示例**：当有 4 个行为时，第 5 个格子是“加号网格”，自动折行到第二行第一列。
> 
> ## 2. 核心概念定义 (Entity Definition)
> *   **行为 (Behavior)**：是主页显示的核心单元。它是“活动”与“标签”的聚合体。
>     *   包含：1个活动（Activity）+ 多个标签（Labels）+ 备注（Notes）。
> *   **活动 (Activity)**：全局预设的类别（如：工作、冥想、阅读）。
> *   **标签 (Label)**：全局预设的维度（如：紧急、深度、琐碎）。
> *   **目标 (Goal)**：待办行为序列，遵循线性顺位逻辑。
> 
> ## 3. 交互与入口逻辑 (Interactions)
> *   **加号网格点击**：弹出“新建行为窗口”。
>     *   功能：选择一个活动，勾选多个标签，输入备注信息。
> *   **活动区标题点击**：弹出“管理/添加活动窗口”。
>     *   用于维护全局的活动库（增删改查活动图标、颜色等）。
> *   **标签区标题点击**：弹出“管理/添加标签窗口”。
>     *   用于维护全局的标签库。
> 
> ## 4. 目标与时间逻辑 (Goal & Time Logic)
> *   **线性顺位执行**：
>     *   目标不支持手动输入“开始/结束时间”。
>     *   **流转机制**：只有当前一个行为被标记为“完成”时，排在首位的“目标”才会自动激活并开始计时，转化为“当前行为”。
> *   **时间度量**：
>     *   **预计花费时间**：单位为“分钟”（Minutes）。
>     *   **实际花费时间**：由程序在行为激活期间自动记录。
> *   **已完成行为的校验**：
>     *   手动录入或补填已完成行为时，**结束时间不得晚于当前时间**。
>     *   **修正逻辑**：若用户输入未来时间，系统自动将其修正为 `Date.now()`。
> 
> ## 5. 数据库字段规范 (Database Schema - Activity/Behavior)
> AI Agent 在创建数据表或对象模型时，必须包含以下字段：
> 
> | 字段名 | 类型 | 说明 |
> | :--- | :--- | :--- |
> | `id` | UUID/String | 唯一标识 |
> | `name` | String | 活动/行为名称 |
> | `icon` | String | 图标标识符或图标名 |
> | `color` | String | 颜色值 (Hex/RGB) |
> | `created_at` | Timestamp | 创建时间 |
> | `is_archived` | Boolean | 是否归档 (Default: false) |
> | `archived_at` | Timestamp | 归档时间 (Nullable) |
> | `total_duration` | Number | 累计耗时 (秒/分) |
> | `label_ids` | Array[ID] | 绑定的标签 ID 列表 |
> | `notes` | Text | 活动/行为备注 |
> | `default_estimate` | Number | 默认预计花费时间 (分钟) |
> 
> ---
> 
> ### 下一步引导思考：
> 
> 既然逻辑已经清晰，为了让 AI 写的代码**更健壮**，我们需要补充最后一点关于**“状态管理”**的细节：
> 
> 1.  **数据的实时性**：当你在“活动区标题”里添加了一个新活动后，主页的“新建行为窗口”里的下拉列表是否需要立即无刷新更新？
> 2.  **计时器的挂起**：如果用户正在进行一个行为（计时中），突然又去点击加号添加了一个“已完成”的过去行为，这会影响当前的计时器吗？
> 3.  **删除逻辑**：如果用户删除了一个已经在主页显示的“活动”，关联的“行为”网格是消失，还是保留文字但显示“活动已删除”？
> 
> **你想如何处理这三个细节？**

---

### 👤 用户 - *2026-04-26 16:07*

> 先完善标签的数据模型，另外需要更正行为、活动、标签是三个模型，行为是类似任务包含时间记录功能的，活动已经描述

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 16:08*

> 非常关键的更正。通过将**行为 (Behavior)**、**活动 (Activity)** 和 **标签 (Label)** 解耦，你的系统实际上形成了一个经典的“实例-模板-元数据”架构。
> 
> 以下是根据你的要求完善后的数据模型逻辑，特别是**标签 (Label)** 的详细设计：
> 
> ### 1. 核心模型三剑客 (The Three Models)
> 
> 为了让 AI Agent 不写错逻辑，我们需要明确这三者的关系：
> *   **活动 (Activity)**：**模板层**。预设的“事由”，如“编程”、“健身”。它定义了默认的图标、颜色和预计时长。
> *   **标签 (Label)**：**属性层**。跨活动的维度，如“深度”、“紧急”、“碎片化”。
> *   **行为 (Behavior)**：**执行层（任务记录）**。这是你主页网格里显示的东西。它引用一个“活动”，关联多个“标签”，并记录**具体发生的时间点**。
> 
> ---
> 
> ### 2. 标签 (Label) 数据模型详细设计
> 标签是全局的元数据，用于对行为进行多维度过滤和统计。
> 
> | 字段名 (Field) | 类型 | 说明 |
> | :--- | :--- | :--- |
> | `id` | UUID | 唯一标识符 |
> | `name` | String | 标签名称 (如：深度工作、自我提升) |
> | `color` | String | 标签背景色 (用于 UI 渲染) |
> | `text_color` | String | 标签文字颜色 (确保对比度) |
> | `icon` | String | 可选图标 |
> | `usage_count` | Number | 该标签被引用的次数 (用于排序常用标签) |
> | `sort_order` | Number | 手动排序权重 |
> | `is_archived` | Boolean | 是否已归档 (隐藏但不删除历史数据) |
> | `created_at` | Timestamp | 创建时间 |
> 
> ---
> 
> ### 3. 三者关联的逻辑补全
> 
> #### **行为 (Behavior) 模型补全**
> 这是 AI 开发中最容易报错的地方，必须明确它是如何引用前两者的：
> 
> *   **activity_id**: 外键，关联 `Activity.id` (必填)。
> *   **label_ids**: 数组，存储多个 `Label.id` (选填)。
> *   **时间记录逻辑**：
>     *   `start_time`: 只有当该行为从“目标”变为“当前行为”时，记录 `Date.now()`。
>     *   `end_time`: 点击完成时记录。
>     *   `actual_duration`: 结束时计算 `end_time - start_time`。
> *   **状态字段**：
>     *   `status`: 枚举值 `[pending (目标), active (进行中), completed (已完成)]`。
> 
> ---
> 
> ### 4. 引导思考：操作层面的细节确认
> 
> 为了确保 AI Agent 在编写增删改查（CRUD）函数时符合你的预期，请回答以下关于“标签管理”的细节：
> 
> **问题 8：关于标签的“解绑”逻辑**
> *   如果在“标签区标题”弹窗里删除了一个全局标签，已经存在的“行为”记录中，这个标签是应该**跟着消失**，还是**保留文本但标记为已失效**？
> 
> **问题 9：添加行为时的标签筛选**
> *   在点击加号弹出“行为窗口”时，标签的选择方式是：
>     1.  从所有全局标签列表中勾选？
>     2.  还是可以像输入框一样，输入不存在的文字就地创建新标签？
> 
> **问题 10：活动的继承性**
> *   当你选择一个“活动”（如：编程）来创建“行为”时，行为是否应该**自动继承**该活动预设的颜色和图标？
> *   **追问**：用户是否可以在特定的某个“行为”里修改图标，而不影响“活动”本身的模板？
> 
> **问题 11：数据库查询逻辑**
> *   主页的 4 列网格，默认是显示“所有历史行为”，还是只显示“今日行为”+“未完成的目标”？（这对数据库 `find` 函数的 Filter 条件至关重要）
> 
> **请针对这些细节给出你的想法，我将据此补充最终的 CRUD 函数逻辑说明。**

---

### 👤 用户 - *2026-04-26 16:13*

> 问题 8：关于标签的“解绑”逻辑 ：保留文本但标记为已失效，若修改名称需要同步更新
> 问题 9：添加行为时的标签筛选：两者都可以，若是输入框，则在备注输入框共存这个逻辑
> 问题 10：活动的继承性： 1.自动继承 2.行为不修改图标，它只是选择展示活动与标签的屏幕
> 问题 11：数据库查询逻辑：今日行为+未完成（未完成目标一般不会很多，因为软件设定的就是一天内按照时间顺序去制定任务，让用户不用考虑安排时间，只需要在意顺序，只需要知道，完成当前行为后你该做的事

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 16:13*

> 这是一个非常深刻的设计决策。通过“只在意顺序，不在意安排时间”，你实际上在设计一个**基于流（Flow）的任务系统**，而不是传统的日历/待办系统。
> 
> 为了让 AI Agent 能够精准实现“标签同步更新”和“顺序流转”逻辑，我们需要细化数据模型间的引用关系。
> 
> ---
> 
> ### 1. 完善后的数据模型 (Refined Models)
> 
> #### **标签模型 (Label)**
> *   `id`: UUID
> *   `name`: String (更新时需同步所有引用)
> *   `color`: String
> *   `is_active`: Boolean (默认为 true，被“删除”后置为 false，但记录保留以供历史查看)
> 
> #### **行为模型 (Behavior) - 核心执行单元**
> *   `id`: UUID
> *   `activity_id`: ID (外键，引用 Activity)
> *   `label_ids`: Array[ID] (引用 Label)
> *   `status`: Enum `['pending', 'active', 'completed']` 
> *   `note`: String (备注)
> *   `start_time`: Timestamp (仅在状态变为 active 时记录)
> *   `end_time`: Timestamp (完成时记录，且校验不可超过当前时间)
> *   `duration`: Number (实际花费时长，分钟)
> *   `estimated_duration`: Number (继承自 Activity 或手动在目标阶段设定)
> *   `sequence`: Number (用于线性排序的权重)
> 
> ---
> 
> ### 2. 数据库逻辑补全 (CRUD & Query)
> 
> #### **查询逻辑：主页网格 (Query Home Grid)**
> AI Agent 需要执行以下逻辑来填充那 4+ 个网格：
> 1.  **Filter**: 
>     *   条件 A：`created_at` 为今天。
>     *   条件 B：`status` 为 `pending` (即：未完成的目标，无论哪天创建的)。
> 2.  **Sort**: 按 `status` (active > completed > pending) 且按 `sequence` 升序排列。
> 3.  **UI 渲染**: 将结果渲染为网格，最后追加一个“加号网格”。
> 
> #### **标签同步与删除 (Label Sync)**
> *   **Update**: 当 Label 表中的 `name` 改变时，由于 Behavior 表存储的是 `label_ids`（引用），前端查询时应通过 `join` 或 `populate` 实时获取最新名称，从而实现“同步更新”。
> *   **Delete**: 软删除。将 Label 的 `is_active` 设为 `false`。在 Behavior 展示时，如果引用的 Label `is_active` 为 `false`，UI 上给标签增加一个“已失效”或“灰色阴影”的样式。
> 
> #### **“备注转化为标签”逻辑 (Note-to-Tag Parsing)**
> 在新建行为窗口中：
> *   AI 需要监听备注输入框。如果用户输入了符合特定格式的内容（例如：`#标签名` 或从下拉框选择），程序需要：
>     1.  检查 Label 表是否存在该名称。
>     2.  不存在则创建新 Label，获取 ID。
>     3.  存在则直接关联该 ID。
> 
> ---
> 
> ### 3. 引导思考：流程自动化逻辑
> 
> 为了实现“线性顺位”的自动化，我们需要定义**状态转换触发器**。
> 
> **问题 12：目标激活触发器**
> *   当你点击当前行为的“完成”按钮时，系统是否应该**自动**将下一个 `pending` 状态的目标变为 `active` 并开始计时？
> *   **追问**：如果用户完成了当前行为，但暂时不想开始下一个（想休息一下），系统是进入“空闲状态”，还是必须有一个行为在计时？
> 
> **问题 13：网格的视觉反馈**
> *   “已完成的行为”在网格中是否需要置灰或缩小？
> *   “当前正在进行的（Active）”行为，是否要在网格中有一个动态的计时效果？
> 
> **问题 14：顺序调整**
> *   既然是线性顺位，用户是否可以手动拖拽网格来改变目标的先后顺序？
> 
> **问题 15：数据清理**
> *   “今日行为”是在午夜自动清空（存档），还是在用户第一次打开第二天的页面时进行结算？
> 
> **请回答这几个关于“流动”的问题，我们将进入最后一步：定义具体的 API/函数接口。**

---

### 👤 用户 - *2026-04-26 16:17*

> 问题 12：目标激活触发器 ：默认模式是自动开始计时，2.可以开启空闲模式（不自动开始
> 问题 13：网格的视觉反馈：对于完成任务，给予激励效果，即一层铂金色边框
> - 当前正在进行的（Active）”行为，是否要在网格中有一个动态的计时效果？ 需要
> 问题 14：顺序调整 ：需要
> 问题 15：数据清理：用户第二天完成前一天跨时区的行为时进行结算，例如睡觉

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 16:18*

> 这套逻辑已经非常成熟了，你正在从一个简单的“任务列表”转向一个**“心流追踪系统”**。特别是“铂金色边框”的激励和“自动流转”的设计，能够极大地降低用户的认知负担。
> 
> 为了让 AI Agent 能够写出完美的 `useBehavior` Hook 或者后端逻辑，我们需要把这几个关键动效和结算逻辑文档化。
> 
> ---
> 
> ### 1. 行为状态机 (Behavior State Machine)
> 
> AI 开发者需要理解行为的生命周期。请确认以下逻辑：
> 
> *   **Pending (目标)**: 只有 `estimated_duration`，没有 `start_time`。
> *   **Active (进行中)**: 记录 `start_time`。前端每秒计算 `currentTime - start_time` 并显示。
> *   **Completed (已完成)**: 记录 `end_time`，计算 `actual_duration`。
>     *   **视觉规范**：渲染“铂金色边框”（可以使用 `conic-gradient` 或特定的图片边框）。
> 
> ### 2. 核心函数逻辑 (Core Functions)
> 
> 我们需要要求 AI 补全以下四个关键函数：
> 
> #### **A. `completeCurrentAndStartNext(currentId)`**
> 1.  更新 `currentId` 的状态为 `completed`，设置 `end_time = Date.now()`。
> 2.  **检查模式**：若 `isIdleMode == false`：
>     *   查找 `sequence` 紧随其后的第一个 `pending` 行为。
>     *   将其状态改为 `active`，设置 `start_time = Date.now()`。
> 3.  **结算检查**：如果当前完成的行为被标记为“清算点（如：睡觉）”，触发 `settleDay()`。
> 
> #### **B. `reorderGoals(newOrderList)`**
> *   使用拖拽库（如 `dnd-kit` 或 `SortableJS`）更新所有 `pending` 状态行为的 `sequence` 字段。
> 
> #### **C. `settleDay()` (跨时区结算)**
> *   **触发时机**：用户完成了一天中最后一个定义的行为。
> *   **逻辑**：将所有状态为 `completed` 的行为移入“历史记录/归档表”，主页 UI 刷新，仅保留未完成的 `pending` 目标作为新的一天的开始。
> 
> ---
> 
> ### 3. 最后几个关键细节（细节决定成败）
> 
> **问题 16：铂金边框的细节**
> *   是所有已完成的都有铂金边框，还是只有“超额完成（实际时间 < 预计时间）”或者“坚持完成”的才有？这涉及到是否需要一个 `achievement_level` 字段。
> 
> **问题 17：空闲模式（Idle Mode）的状态**
> *   当处于空闲模式时，网格中是否应该显示一个明显的“休息中”或者“等待开始”的占位符，还是加号网格变成“开始下一个”？
> 
> **问题 18：关于“跨天”行为的归属**
> *   如果我凌晨 1 点才完成昨晚 23 点开始的“睡觉”行为。
> *   **统计逻辑**：这个“睡觉”时长是计入“昨天”的成就，还是“今天”的开始？
> 
> **问题 19：删除行为的影响**
> *   如果删除序列中间的一个 `pending` 目标，后续目标的 `sequence` 是自动前移填充，还是保持空隙？
> 
> **请对这四个问题进行最后的定调，我将为你生成最终的《AI Agent 开发全量 Prompt 文档》。**

---

### 👤 用户 - *2026-04-26 16:24*

> 1. 行为状态机 (Behavior State Machine) 确认 Completed (已完成)补充，铂金色边框只对此前是目标的行为生效，表示激励用户有计划的态度
> 2. 核心函数逻辑 (Core Functions) 确认
> 问题 16：铂金边框的细节：铂金色边框只对此前是目标的行为生效，可以用你的逻辑achievement_level进行判断，并且实际时间与预计时间越相近，效果越好
> 问题 17：空闲模式（Idle Mode）的状态 ：变成开始下一个，占位符不太重要，这个比较容易更改
> 问题 18：关于“跨天”行为的归属 ： 昨天的成就
> 问题 19：删除行为的影响： 自动前移填充

---

### 🤖 fackgemini|gemini-3-flash-preview - *2026-04-26 16:24*

> 太棒了，这套设计逻辑非常自洽且具有很强的心理暗示作用（通过铂金边框奖励计划性）。
> 
> 现在，我已经将我们所有的对话整理成了一份**《feature>home 模块开发指令集》**。你可以直接将以下内容发送给 AI Agent（如 Cursor, AutoGPT 或 GPT-4），它将能够理解所有复杂的逻辑并写出符合预期的代码。
> 
> ---
> 
> # AI Agent 开发任务书：feature>home 模块
> 
> ## 1. 数据模型定义 (Database Models)
> 
> ### Activity (活动模板)
> - `id`, `name`, `icon`, `color`, `default_estimate` (分钟)
> - `is_archived`, `created_at`
> 
> ### Label (全局标签)
> - `id`, `name`, `color`, `is_active` (软删除标识)
> - *注意：若修改 name，所有引用此 id 的 Behavior 需同步显示新名称。*
> 
> ### Behavior (行为记录)
> - `id`, `activity_id` (关联 Activity)
> - `label_ids` (数组，关联 Label)
> - `status`: `pending` (目标), `active` (进行中), `completed` (已完成)
> - `sequence`: Number (线性排序权重)
> - `start_time`, `end_time`: Timestamp
> - `estimated_duration`: Number (预计耗时)
> - `actual_duration`: Number (实际耗时)
> - `notes`: Text
> - `achievement_level`: Number (0-100, 衡量预计与实际时间的接近程度)
> - `was_planned`: Boolean (创建时若为 pending 则为 true，用于铂金边框判定)
> 
> ## 2. UI 渲染逻辑 (Grid Logic)
> 
> - **布局**：固定 4 列网格。
> - **动态序列**：渲染 `Behavior` 列表，在序列末尾追加一个 **“加号占位网格”**。
> - **初始态**：若无行为，仅显示 1 个加号网格（占据 1/4 宽度）。
> - **完成态视觉**：
>     - 若 `was_planned` 为 true 且已完成：显示 **铂金色边框**。
>     - **成就算法**：边框亮度/特效强度与 `abs(actual_duration - estimated_duration)` 成反比（越接近，效果越强）。
> - **执行态视觉**：正在进行的网格需显示动态计时器（`Date.now() - start_time`）。
> 
> ## 3. 核心功能逻辑 (Business Logic)
> 
> ### A. 状态流转 (State Transition)
> - **完成当前任务**：
>     - 设置 `end_time = Date.now()` (校验：不可超过当前时间，超规则修正)。
>     - 计算 `actual_duration`。
>     - **自动触发**：若非空闲模式，将 `sequence` 下一个的 `pending` 任务设为 `active` 并记录 `start_time`。
>     - **空闲模式**：加号网格变为“开始下一个”按钮，点击后激活。
> 
> ### B. 顺序维护 (Sequence Management)
> - **删除逻辑**：删除中间的 `pending` 行为时，后续行为的 `sequence` 必须自动前移（填充空隙）。
> - **拖拽逻辑**：支持对 `pending` 状态的网格进行拖拽排序，实时更新数据库 `sequence`。
> 
> ### C. 跨天结算 (Settlement)
> - **规则**：用户在第二天凌晨完成前一天的最后一个行为（如“睡觉”）时，该行为的统计数据归属于**前一天**。
> - **触发**：结算后，主页仅保留剩余的 `pending` 目标。
> 
> ### D. 交互入口
> - **点击加号网格**：弹出“新建行为窗口”，支持从活动库选活动，从标签库选标签（或备注框输入 `#` 快捷创建并关联标签）。
> - **点击标题（活动/标签）**：分别弹出管理窗口，支持全局增删改查。
> 
> ## 4. CRUD 函数约束
> 1. `getHomeBehaviors()`: 查询 `created_at` 为今天 OR `status == 'pending'` 的所有行为，按 `status` 和 `sequence` 排序。
> 2. `addBehavior(data)`: 初始 `sequence` 设为 `max(sequence) + 1`。
> 3. `completeBehavior(id)`: 包含 achievement_level 计算与后续自动激活逻辑。
> 4. `syncLabels(labelId, newName)`: 确保 UI 层通过 ID 关联，实现名称实时同步。
> 
> ---
> 
> ### **引导思考结语**：
> 这个文档涵盖了从 UI 到数据库、从逻辑到心理激励的所有细节。
> **你现在可以尝试将这段指令发给你的 AI 助手，看看它生成的代码是否符合你的直觉。** 
> 
> 如果在开发过程中，AI 对“铂金边框的 CSS 实现”或“拖拽排序的索引更新”报错，你可以随时回来，我会帮你拆解更细的函数逻辑。你需要现在就开始测试第一份代码吗？

---

